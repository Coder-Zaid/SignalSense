'use client';

// Temporarily disabled to prevent build errors
// Will be re-enabled once @react-three/fiber is properly set up
export function PerformanceMonitor() {
  // Temporary empty component
  return null;
}
